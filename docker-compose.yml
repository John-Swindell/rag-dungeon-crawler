services:
  api:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - APP_ENV=development
  # mongo:
  #   image: mongo:7
  #   ports:
  #     - "27017:27017"
